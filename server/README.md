

// Express = Express is backend framework for writing rest apis , middleware.
// It runs top of node js
// Express.js (or simply Express) is a minimal, fast, and
//  flexible backend web application framework built on top of Node.js. 
// It acts as a layer above Node.js to simplify the process of building 
// web applications, APIs, and microservices 
// by reducing the amount of complex boilerplate code required

//    MONGOOSE => is library for writing schema in node js and mongodb

// Mongoose is an Object Data Modeling (ODM) library for MongoDB and Node.js.
//  It acts as a structured bridge between your JavaScript code and the database,
//   managing data relationships, providing schema 
// validation, and translating JavaScript objects into MongoDB documents.


// Nodemon => it runs our server autometic,
// Nodemon is a popular command-line development utility that automatically
//  restarts your Node.js application
//  whenever it detects file changes in your project directory

// dotevn
// dotenv is a popular Node.js library that loads environment variables from a .env
//  file directly into process.env

// cors ==> it enable your backend apis to use in frontend for prevent cors error
// in your browser


// In Node.js, CORS stands for Cross-Origin Resource Sharing,
//  and it is handled using a popular middleware package called cors. 
// It allows or restricts requested resources on a 
// web server depending on where the HTTP request was initiated
